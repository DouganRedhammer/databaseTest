@model databaseTest.Models.MentorMentee

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>MentorMentee</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.MentorId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.MentorId)
            @Html.ValidationMessageFor(model => model.MentorId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.MenteeId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.MenteeId)
            @Html.ValidationMessageFor(model => model.MenteeId)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
